# spidey-token-estimator
Pre-upload token budget estimator for Spidey V2 tasks. Extracts text from PDF, DOCX, XLSX, CSV, TXT, and MD files, estimates per-file and total context tokens, flags risk against a 1M-token cap, and highlights the largest contributors so CBs can trim files or use excerpts before submission.
